name: build binaries

on:
  workflow_dispatch:
    inputs:
      release_tag:
        description: Existing release tag to upload build outputs to. Leave empty to skip release upload.
        required: false
        default: latest
        type: string
      overwrite_release_assets:
        description: Replace existing release assets with the same filenames.
        required: false
        default: true
        type: boolean

permissions:
  contents: write
  id-token: write

jobs:
  prepare-release:
    if: ${{ inputs.release_tag != '' }}
    runs-on: ubuntu-latest
    env:
      RELEASE_TAG: ${{ inputs.release_tag }}
      RELEASE_NAME: Latest build
      TARGET_REPO: ${{ github.repository }}
      GH_TOKEN: ${{ github.token }}

    steps:
      - name: Confirm target release exists
        shell: bash
        run: |
          set -euo pipefail
          if gh release view "$RELEASE_TAG" --repo "$TARGET_REPO" >/dev/null 2>&1; then
            gh release edit "$RELEASE_TAG" --repo "$TARGET_REPO" --prerelease --latest=false --title "$RELEASE_NAME"
          else
            gh release create "$RELEASE_TAG" --repo "$TARGET_REPO" --prerelease --latest=false --title "$RELEASE_NAME" --notes ""
          fi

  linux-intel:
    needs: prepare-release
    if: ${{ always() && (needs.prepare-release.result == 'success' || needs.prepare-release.result == 'skipped') }}
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

    steps:
      - name: Checkout repo
        uses: actions/checkout@v5

      - name: Setup Node
        uses: actions/setup-node@v5
        with:
          node-version: 22.13.0

      - name: Install dependencies
        run: npm install

      - name: Build TypeScript output
        run: npm run build

      - name: Build Electron app (Linux intel)
        run: node scripts/build-linux.js --x64

      - name: Rename Linux artifacts
        run: npm run rename:linux

      - name: Upload Linux assets to GitHub release
        if: ${{ inputs.release_tag != '' }}
        shell: bash
        env:
          RELEASE_TAG: ${{ inputs.release_tag }}
          TARGET_REPO: ${{ github.repository }}
          OVERWRITE_RELEASE_ASSETS: ${{ inputs.overwrite_release_assets }}
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          if [ "$OVERWRITE_RELEASE_ASSETS" = "true" ]; then
            release_assets=()
            while IFS= read -r asset; do
              release_assets+=("$asset")
            done < <(gh release view "$RELEASE_TAG" --repo "$TARGET_REPO" --json assets --jq '.assets[].name')
            for asset in "${release_assets[@]}"; do
              case "$asset" in
                *.AppImage|latest-linux.yml)
                  gh release delete-asset "$RELEASE_TAG" "$asset" --repo "$TARGET_REPO" --yes
                  ;;
              esac
            done
          fi
          files=(
            build/output/*.AppImage
            build/output/latest-linux.yml
          )
          cmd=(gh release upload "$RELEASE_TAG" --repo "$TARGET_REPO")
          if [ "$OVERWRITE_RELEASE_ASSETS" = "true" ]; then
            cmd+=(--clobber)
          fi
          cmd+=("${files[@]}")
          "${cmd[@]}"

  windows-intel:
    needs: prepare-release
    if: ${{ always() && (needs.prepare-release.result == 'success' || needs.prepare-release.result == 'skipped') }}
    runs-on: windows-latest
    environment: signing
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

    steps:
      - name: Checkout repo
        uses: actions/checkout@v5

      - name: Setup Node
        uses: actions/setup-node@v5
        with:
          node-version: 22.13.0

      - name: Install dependencies
        run: npm install

      - name: Build TypeScript output
        run: npm run build

      - name: Build Electron app (Windows intel)
        run: npx electron-builder --win nsis portable --x64 --publish never

      - name: Rename Windows artifacts
        run: npm run rename:win

      - name: Azure login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.AZURE_APPLICATION_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Sign Windows artifacts
        uses: azure/artifact-signing-action@v1
        with:
          endpoint: https://plc.codesigning.azure.net/
          signing-account-name: dusadrian
          certificate-profile-name: electron-profile
          files-folder: ${{ github.workspace }}\build\output
          files-folder-filter: exe
          file-digest: SHA256
          timestamp-rfc3161: http://timestamp.acs.microsoft.com
          timestamp-digest: SHA256

      - name: Refresh Windows updater metadata
        run: npm run refresh:win-update

      - name: Upload Windows assets to GitHub release
        if: ${{ inputs.release_tag != '' }}
        shell: bash
        env:
          RELEASE_TAG: ${{ inputs.release_tag }}
          TARGET_REPO: ${{ github.repository }}
          OVERWRITE_RELEASE_ASSETS: ${{ inputs.overwrite_release_assets }}
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          if [ "$OVERWRITE_RELEASE_ASSETS" = "true" ]; then
            release_assets=()
            while IFS= read -r asset; do
              release_assets+=("$asset")
            done < <(gh release view "$RELEASE_TAG" --repo "$TARGET_REPO" --json assets --jq '.assets[].name')
            for asset in "${release_assets[@]}"; do
              case "$asset" in
                *.exe|*.exe.blockmap|latest.yml)
                  gh release delete-asset "$RELEASE_TAG" "$asset" --repo "$TARGET_REPO" --yes
                  ;;
              esac
            done
          fi
          files=(
            build/output/*.exe
            build/output/*.exe.blockmap
            build/output/latest.yml
          )
          cmd=(gh release upload "$RELEASE_TAG" --repo "$TARGET_REPO")
          if [ "$OVERWRITE_RELEASE_ASSETS" = "true" ]; then
            cmd+=(--clobber)
          fi
          cmd+=("${files[@]}")
          "${cmd[@]}"
