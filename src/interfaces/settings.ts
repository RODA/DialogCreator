import { DialogProperties } from './dialog';
export interface EditorSettings {
    fontSize: number;
    fontFamily: string;
    maxWidth: number;
    maxHeight: number;
    dialog: DialogProperties;
}

